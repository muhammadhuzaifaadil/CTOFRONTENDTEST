// // src/hooks/useDirection.ts
// "use client";

// import { useLocale } from "next-intl";

// export default function useDirection() {
//   const locale = useLocale();
//   const dir = locale === "ar" ? "rtl" : "ltr";
//   return { dir, locale, isRtl: dir === "rtl" };
// }
